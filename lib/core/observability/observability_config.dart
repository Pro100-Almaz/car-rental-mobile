/// Feature flags for observability tooling.
///
/// Set [kEnableCrashlytics] = true and add `firebase_crashlytics` to
/// pubspec.yaml when the Firebase project is provisioned.
///
/// Set [kEnableAnalytics] = true and add `firebase_analytics` to
/// pubspec.yaml when ready to collect analytics events.
const bool kEnableCrashlytics = false;
const bool kEnableAnalytics = false;
